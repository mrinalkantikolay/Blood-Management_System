# Blood Management System — dependency & install helper

# Node version
# Recommended: Node.js v16 or newer

# Install backend dependencies and start
cd backend
npm install

# Configure backend env: copy/backend/.env.example -> backend/.env and set DB credentials

# Run backend
npm start

# Install frontend dependencies and start (in a separate terminal)
cd ../frontend
npm install
npm start

# Notes
# - This project uses npm and package.json files in `backend/` and `frontend/` as the canonical manifests.
# - If you plan to store large uploaded files, consider enabling Git LFS for `backend/uploads/`.
